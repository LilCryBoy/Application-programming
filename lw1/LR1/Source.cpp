#include <iostream>
#include <math.h>
#include <cmath>


int main() 
{
	double PI = 3.14159265;
	double y, z;
	y = 2 * (pow(sin(3 * PI), 2)) * (pow(cos(5 * PI), 2));
	z = 1/4 - 1/4 * sin(5/2 * PI - 
	std::cout << y << std::endl;
	system("pause");
}